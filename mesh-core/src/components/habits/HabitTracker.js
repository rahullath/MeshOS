import React from 'react';

const HabitTracker = ({ habit }) => {
  return (
    <div>
      <h3>{habit.name}</h3>
      {/* Implement habit tracking UI here */}
    </div>
  );
};

export default HabitTracker;
